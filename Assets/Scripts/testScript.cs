using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testScript : MonoBehaviour
{
    //public Transform from;
    //public Transform to;

    //private void Update()
    //{
    //    transform.rotation = Quaternion.Slerp(from.localRotation, to.localRotation, Time.time);
    //    transform.position = Vector3.Lerp(from.localPosition, to.localPosition, Time.time);
    //}


    //private void Update()
    //{
    //    float increasingValue = Mathf.Lerp(0, 1, Time.time);
    //    float decreasingValue = Mathf.Lerp(1, 0, Time.time);

    //    Debug.LogFormat("My Increasing Value : {0} and my decreasing value : {1}", increasingValue, decreasingValue);
    //}
}
